import { MembersDetailPipe } from './members-detail.pipe';

describe('MembersDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new MembersDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
